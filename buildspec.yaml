version: 0.2

phases:

  pre_build:
    commands:
      - echo Entered prebuild phase


    build:
      commands:
        - echo Entered build on
        - mvn clean package
        - dir target


    post_build:
      commands:
        - echo Build completed


  artifacts:
    files:
      - 'target/*.jar'
    discard-paths: yes